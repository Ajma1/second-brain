---
type: issue
source: linear
date: 2026-06-12
url: "https://linear.app/modern-agency-sales/issue/MAE-233/e8-identity-tenancy-and-billing"
title: "[E8] Identity, Tenancy & Billing"
identifier: "MAE-233"
status: "Backlog"
assignee: ""
team: "MAE"
---

# [E8] Identity, Tenancy & Billing

- **Identifier:** MAE-233
- **Status:** Backlog
- **Assignee:** —
- **Team:** MAE
- **Updated:** 2026-06-12
- **URL:** https://linear.app/modern-agency-sales/issue/MAE-233/e8-identity-tenancy-and-billing

**Owner:** Ajmal · horizontal — spine SHIPPED in PRs #8–#10

Stories:

1. ✅ As any caller, identity comes from `ctx.auth`; no public function accepts a caller-supplied user/tenant id (lint-enforced).
2. ✅ As a user, I'm provisioned idempotently into a personal workspace with owner membership.
3. As an agency owner, I can invite ghostwriters and clients with role-scoped access.
4. As a client, I only ever see my own workspace, brain, and content.
5. As a platform operator, every admin action is audit-logged (audit-log component).
6. As an agency owner, I can manage billing per seat/client — with usage/credit-based pricing as a first-class option (Mark: Ghost Base is "hella expensive"; competitive pricing matters). [MK]

---

## Prior art (verified 2026-06-12)

**Identity + provisioning shipped (stories 1–2 DONE). Remaining stories have strong prior art in v2-3.**

**Auth wrappers — shipped in clean repo PRs #8–#10 (DONE):**

* Clean repo has `ctx.auth`-derived identity + idempotent provisioning. Stories 1–2 are complete.

**Prior art reference for the shipped pattern (PORT-AS-IS was the source):**

* `maestro-v2-3` `packages/convex/convex/lib/auth/` — 15 modules: `requireUserAccess.ts`, `wrappers.ts` (`workspaceQuery/Mutation/Action`), `provisionUser.ts`, `requireWorkspaceAccess.ts`, `requireClientUserAccess.ts`, `agencyWrappers.ts`, `requireSuperAdmin.ts` — this is the auth architecture that PRs #8–#10 ported and hardened

**Role-scoped invitations (PORT-WITH-REWORK):**

* `maestro-v2-3` `packages/convex/convex/agency/seats.ts`, `agency/clients.ts` — invite flows for ghostwriters and clients
* `maestro-v2-3` `apps/web/app/invite/[token]/client-invite-acceptance.tsx` — hardened invite acceptance (406 lines, 22 tests)
* `maestro-v2-3` `lib/__tests__/agency.crossBoundary.test.ts` — cross-boundary isolation tests (PORT-AS-IS as spec)

**Client data isolation (PORT-WITH-REWORK):**

* `maestro-v2-3` `lib/auth/requireClientUserAccess.ts` — client-scoped access wrappers
* `maestro-v2-3` `lib/__tests__/v1Killer.test.ts` — provisions two users, asserts User B gets `NO_WORKSPACE_ACCESS` reading User A's post; also meta-lint asserting no public fn exposes raw `workspaceId` args (PORT-AS-IS)

**Admin audit log (PORT-WITH-REWORK):**

* `maestro-v2-3` `packages/convex/convex/admin/auditLogs.ts`, `lib/audit.ts` — audit log infrastructure
* `maestro-v2-3` `lib/__tests__/adminAuditCoverage.test.ts` — meta-lint asserting every admin action is audit-logged (PORT-AS-IS)

**Billing (REBUILD-AS-SPEC):**

* `maestro-v2-3` `packages/convex/convex/lib/billing.ts` — billing module (seat-based). Needs redesign to first-class usage/credit-based pricing. Use as reference, not direct port.
