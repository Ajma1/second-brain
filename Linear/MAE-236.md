---
type: issue
source: linear
date: 2026-06-12
url: "https://linear.app/modern-agency-sales/issue/MAE-236/eh-2-assertselfwhenauthenticated-becomes-fail-closed"
title: "[EH-2] assertSelfWhenAuthenticated becomes fail-closed"
identifier: "MAE-236"
status: "Backlog"
assignee: ""
team: "MAE"
---

# [EH-2] assertSelfWhenAuthenticated becomes fail-closed

- **Identifier:** MAE-236
- **Status:** Backlog
- **Assignee:** —
- **Team:** MAE
- **Updated:** 2026-06-12
- **URL:** https://linear.app/modern-agency-sales/issue/MAE-236/eh-2-assertselfwhenauthenticated-becomes-fail-closed

`assertSelfWhenAuthenticated` must be fail-closed: if auth is absent or the caller does not match the target user, the function must throw, never silently pass.

Part of the EH hotfix series on maestro-v2-3.

---

## Prior art (verified 2026-06-12)

* **maestro-v2-3** `origin/main`: `packages/convex/convex/agents/orchestrator.ts:≈975` — `assertSelfWhenAuthenticated` as currently defined: `if (identity === null) return;` — unauthenticated callers pass silently. This is the exact function to make fail-closed (throw instead of return). Called at `:≈990` and `:≈1060`.
* **maestro-v2-3** `origin/main`: `packages/convex/convex/lib/auth/requireUserAccess.ts:≈84` — `assertSelfUserUnlessDemo` is the fail-closed counterpart (throws on mismatch); use as the pattern for the fix. Verdict: **PORT-WITH-REWORK** — one-line change but must verify all call sites behave correctly after the throw.
