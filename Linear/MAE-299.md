---
type: issue
source: linear
date: 2026-06-20
url: "https://linear.app/modern-agency-sales/issue/MAE-299/debt-pre-macroscope-audit-findings-judgment-level-debt-that-merged"
title: "[DEBT] Pre-Macroscope audit findings — judgment-level debt that merged before the blocking gate"
identifier: "MAE-299"
status: "Done"
assignee: ""
team: "MAE"
---

# [DEBT] Pre-Macroscope audit findings — judgment-level debt that merged before the blocking gate

- **Identifier:** MAE-299
- **Status:** Done
- **Assignee:** —
- **Team:** MAE
- **Updated:** 2026-06-20
- **URL:** https://linear.app/modern-agency-sales/issue/MAE-299/debt-pre-macroscope-audit-findings-judgment-level-debt-that-merged

Full-tree codex audit of main HEAD (2026-06-12), since almost all code PRs (#2–#24) merged before the Macroscope judgment gate existed. Triaged below. Each REAL one becomes a small fix PR through the now-blocking contract-review gate.

## 🔴 Real, needs fixing

1. **posts not workspace-scoped** — `schema/posts.ts:12` has `authorId: v.id("users")` + `by_author` index, NO workspaceId. Posts bypass the tenancy spine (the v2-3 IDOR class). Decision needed: workspace-scope it, or delete (posts.ts may be the superseded publishPost exemplar — the product uses batches/pieces now). **\[Tim: scope or delete?\]**
2. **contentRules ZFC** — `checks/contentRules.ts:133` encodes AI-tell vocabulary + hollow-hook *judgment* as keyword tables. Banned-phrase lists are legitimately deterministic (OK); semantic hook/tone judgment as keywords is a ZFC violation → move to a model call. Needs per-rule triage.
3. **inventory policy in capability** — `capabilities/inventory.ts:24` owns product cadence/relevant-status policy; move to domain/workspace config, capability stays DB plumbing.
4. **fake DB ignores withIndex** — `adapters/auth.test.ts:47` — the in-memory fake ignores `withIndex` predicates, so tenancy/isolation tests pass even if the workspace filter is removed. **This undermines confidence in every isolation guarantee.** Enforce recorded eq-filters or use Convex contract tests. (HIGH — false-confidence in the security tests.)

## 🟡 Real, minor

5. `capabilities/workspaces.ts:117` — membership filtering/shaping in capability impl; move predicates/projections to domain.
6. `capabilities/inventory.test.ts:64` — asserts the mock's bookkeeping not behavior (same class as the #32 fake test); seed mixed data, assert observable returns.

## Filtered out

* 🟡 `apps/web/navigation/routes.ts` — STALE (already deleted in #31; audit ran on older main).
* 🔴 `adapters/llm.ts` spend attribution — IN FLIGHT (PR #32 spend cap).
* 🟡 `workspace-sidebar.tsx` demo identity — IN FLIGHT (slice 1, PR #28).
