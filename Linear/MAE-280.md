---
type: issue
source: linear
date: 2026-06-12
url: "https://linear.app/modern-agency-sales/issue/MAE-280/e8-1-ctxauth-identity-no-public-function-accepts-caller-supplied"
title: "[E8-1] ctx.auth identity — no public function accepts caller-supplied user/tenant id"
identifier: "MAE-280"
status: "In Progress"
assignee: "Tim Keen"
team: "MAE"
---

# [E8-1] ctx.auth identity — no public function accepts caller-supplied user/tenant id

- **Identifier:** MAE-280
- **Status:** In Progress
- **Assignee:** Tim Keen
- **Team:** MAE
- **Updated:** 2026-06-12
- **URL:** https://linear.app/modern-agency-sales/issue/MAE-280/e8-1-ctxauth-identity-no-public-function-accepts-caller-supplied

As any caller, identity comes from `ctx.auth`; no public function accepts a caller-supplied user/tenant id (lint-enforced).

**Status:** DONE — shipped in PRs #8–#10.

---

## Prior art (verified 2026-06-12)

* **maestro-v2-3** `origin/main`: `packages/convex/convex/lib/auth/requireUserAccess.ts` — `assertSelfUserUnlessDemo` + `requireUser` + `assertSelfUser` — the auth-first pattern that was the model. `packages/convex/convex/lib/auth/wrappers.ts` — `workspaceQuery/Mutation/Action` + `accountAction` auth wrappers. Verdict: **PORT-AS-IS (core)** — these 15 auth modules are explicitly the correct architecture; drop `legacyPublicBuilders.ts`.
* **maestro-v2-3** `origin/main`: `packages/convex/convex/lib/__tests__/v1Killer.test.ts` — meta-lint test asserting no public fn exposes raw `workspaceId` args outside tenant wrappers. Verdict: **PORT-AS-IS** — this meta-test pattern is uniquely valuable for enforcement.
