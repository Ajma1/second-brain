---
type: issue
source: linear
date: 2026-06-12
url: "https://linear.app/modern-agency-sales/issue/MAE-235/eh-1-auth-gate-sendcopilotmessage-sendcopilotvoicemessage-draftpost"
title: "[EH-1] Auth-gate sendCoPilotMessage / sendCoPilotVoiceMessage / draftPost / agents/email.ts"
identifier: "MAE-235"
status: "Backlog"
assignee: ""
team: "MAE"
---

# [EH-1] Auth-gate sendCoPilotMessage / sendCoPilotVoiceMessage / draftPost / agents/email.ts

- **Identifier:** MAE-235
- **Status:** Backlog
- **Assignee:** —
- **Team:** MAE
- **Updated:** 2026-06-12
- **URL:** https://linear.app/modern-agency-sales/issue/MAE-235/eh-1-auth-gate-sendcopilotmessage-sendcopilotvoicemessage-draftpost

Auth-gate the following public Convex functions so they require an authenticated caller:

* `sendCoPilotMessage`
* `sendCoPilotVoiceMessage`
* `draftPost`
* `agents/email.ts`

Part of the EH hotfix series on maestro-v2-3.

---

## Prior art (verified 2026-06-12)

* **maestro-v2-3** `origin/main`: `packages/convex/convex/agents/orchestrator.ts:≈601` (`sendCoPilotMessage`) and `:≈614` (`sendCoPilotVoiceMessage`) — both public actions taking raw `userId: v.id("users")` with zero auth check; any caller can chat/draft as any user. Verdict: **REBUILD-AS-SPEC** — add `ctx.auth` identity derivation, remove caller-supplied userId from these surfaces.
* **maestro-v2-3** `origin/main`: `packages/convex/convex/agents/content.ts:≈293` (`draftPost`) — public action, raw `userId`, no guard. `packages/convex/convex/agents/email.ts` — zero auth guards on any exported function.
* **maestro-v2-3** `origin/main`: `packages/convex/convex/lib/auth/requireUserAccess.ts:≈84` (`assertSelfUserUnlessDemo`) — the correct auth-first pattern to follow when porting; same file has `requireUser` + `assertSelfUser` as building blocks.
