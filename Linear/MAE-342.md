---
type: issue
source: linear
date: 2026-06-17
url: "https://linear.app/modern-agency-sales/issue/MAE-342/e1-29-co-edit-docsidebar-three-tab-suggestions-sources-process-accept"
title: "[E1-2.9] Co-edit DocSidebar — three-tab (Suggestions / Sources / Process) + accept-all"
identifier: "MAE-342"
status: "In Review"
assignee: ""
team: "MAE"
---

# [E1-2.9] Co-edit DocSidebar — three-tab (Suggestions / Sources / Process) + accept-all

- **Identifier:** MAE-342
- **Status:** In Review
- **Assignee:** —
- **Team:** MAE
- **Updated:** 2026-06-17
- **URL:** https://linear.app/modern-agency-sales/issue/MAE-342/e1-29-co-edit-docsidebar-three-tab-suggestions-sources-process-accept

PR-9 of the MAE-240 co-edit studio stack. Stacks on PR #219 (`feat/coedit-annotations-cap-MAE-341`).

Right-rail sidebar that consumes the annotations + editEvents reactive subscriptions. Three tabs:

* **Suggestions** — list of pending annotations (from `listPending`), each rendered via `AnnotationCard` with Accept/Reject buttons that call `accept`/`reject` capabilities from PR-8.
* **Sources** — empty state for now (depends on MAE-290 grounding + future market-claim pipeline).
* **Process** — vertical timeline reading from `editEvents` (workspaceQuery `listForPiece`).

Plus the `AcceptAllByAuthor` button + `AuthorFilterChips` (Agent / Me / All).

Files:

* `apps/web/components/studio/DocSidebar.tsx` — NEW. notion-kit Tabs primitive. Three tab panels.
* `apps/web/components/studio/AnnotationCard.tsx` — NEW. Card per pending annotation.
* `apps/web/components/studio/AuthorFilterChips.tsx` — NEW. Three chips.
* `apps/web/components/studio/AcceptAllByAuthorButton.tsx` — NEW. Calls `acceptAllByAuthor`.
* `apps/web/components/studio/ProvenanceTimeline.tsx` — NEW. Vertical spine + numbered step rows.
* `apps/web/components/studio/StudioSurface.tsx` — EXTEND to mount the DocSidebar.
* `packages/convex/convex/capabilities/editEvents.ts` — EXTEND. Add `listForPiece` + `record` internal mutation.
* `packages/convex/convex/capabilities/annotations.ts` — EXTEND. Add `acceptAllByAuthor`.

OUT OF SCOPE: agent chat rail (PR-10), workflow integration (PR-11), view-mode toggle.
