---
type: pr
source: github
date: 2026-06-08
url: https://github.com/modernagencysales/maestro-agent-planning/pull/45
title: "Post-writing workflow (inner loop): hook best-of-N → body → lint → judge → revise"
repo: modernagencysales/maestro-agent-planning
number: 45
state: closed
author: kimprobably
---

# Post-writing workflow (inner loop): hook best-of-N → body → lint → judge → revise

PR #45 in modernagencysales/maestro-agent-planning — closed — by kimprobably — [https://github.com/modernagencysales/maestro-agent-planning/pull/45](https://github.com/modernagencysales/maestro-agent-planning/pull/45)

## Summary

Plan 1 of the post-writing flow: a durable `@convex-dev/workflow` that turns a topic into a publish-ready draft, reusing the existing `convex/lib/methodology` leaf functions.

Pipeline: **resolve context (agency methodology ⊕ client overlay) → hooks (best-of-N, one per archetype) → hook judge → winner → body (delivers on the hook) → deterministic lint → AI judge (rule-scoped + coherence) → bounded revise loop (keep-best) → persisted `awaiting_review` candidate with attribution.**

- 6 new pure leaf functions (TDD'd with `mockGenerate`): `hookArchetypes`, `draftHooks`, `judgeAndSelectHooks`, `draftBody`, `score`/`coherenceRule`/`pickBest`, `revisePost`.
- Convex layer: `postRuns` table, step actions, the workflow handler + `startPostWriting`, failed-run handling via `onComplete`.
- Spec + plan + live-verification note under `docs/superpowers/`.

## Verification

- **76/76** methodology unit tests pass; `convex dev` codegen/typecheck clean.
- **Live end-to-end run** (`anthropic/claude-haiku-4-5` via OpenRouter): reached `awaiting_review`, best-of-N hooks judged + winner selected, body opened with the hook + passed the coherence check, deterministic lint captured (`lintFixes: ["no-em-dash"]`), score 0.96, client voice preserved. Details in `docs/superpowers/notes/2026-06-08-post-writing-verification.md`.
- Two-stage code review per phase (spec + quality); review caught and fixed 2 real bugs (failed-run status; lint-finding attribution).

## Scope / deferred

Inner loop only. Deferred to follow-on plans: unified scoped rule model, per-template mode config, agent MCP surface, and **Braintrust prompt-versioning + offline A/B** (next).

## Test plan

- [ ] `npx vp test convex/lib/methodology` (76 passing)
- [ ] `npx convex dev --once` (codegen/typecheck clean)
- [ ] `npx convex run postWriting:startPostWriting <args>` then `getPostRun` → `awaiting_review` candidate

🤖 Generated with [Claude Code](https://claude.com/claude-code)
