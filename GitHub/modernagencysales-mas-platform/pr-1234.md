---
type: pr
source: github
date: 2026-05-13
url: https://github.com/modernagencysales/mas-platform/pull/1234
title: "fix(launchpad): shared async dispatch + stale-running watchdog"
repo: modernagencysales/mas-platform
number: 1234
state: closed
author: Ajma1
---

# fix(launchpad): shared async dispatch + stale-running watchdog

PR #1234 in modernagencysales/mas-platform — closed — by Ajma1 — [https://github.com/modernagencysales/mas-platform/pull/1234](https://github.com/modernagencysales/mas-platform/pull/1234)

## Summary
- Adds shared `dispatchAsyncTask` helper so all 6 async LaunchPad step handlers route through one chokepoint; throws on empty `handle.id` (the silent-enqueue failure mode behind the live `intelligence_build` stall on `ajmal@maestrogtm.com`).
- Adds server-side watchdog cron (`launch-pad-watchdog`, every 60s) that force-fails `running` rows past their declared `maxDuration + 60s` grace via the real `transitionStep` writer — notifications + downstream `autoTrigger` unlock fire as if the task had reported failure naturally.
- Wires `onPhase` into `runIntelligencePipeline` and writes `phase_results` + `current_phase` into `launch_pad_step_state.result` so the LaunchPad UI's 4 sub-bullets (Gathering sources → Researching web → Analyzing LinkedIn → Building entity profiles) tick live. Blueprint short-circuit synthesizes immediate `completed` transitions for the fast path.
- Tightens the Blueprint short-circuit gate via `isBlueprintConnected()` (accepts boolean `true` OR case-insensitive string `"true"`).

Plan: `~/.claude/plans/okay-so-fixed-the-cached-meerkat.md` (Phases 2/3/4/5).

## Test plan
- [x] `pnpm turbo run typecheck --filter=@maestro/app --filter=@maestro/gtm`
- [x] handlers / dispatch / watchdog / intelligence-build-short-circuit vitests — 45/45 pass
- [ ] Deploy trigger tasks (`./scripts/deploy.sh trigger-app`) so the watchdog cron is live
- [ ] Vercel deploy to picks up callback + dispatch changes
- [ ] Manual: flip live stuck row to `failed`, click Retry, observe the 4 sub-bullets tick + step → `done`
- [ ] Phase 6 follow-up — walk all 12 steps end-to-end on a fresh staging user

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->
## Summary by CodeRabbit

* **New Features**
  * Watchdog service to detect and auto-fail stale running steps
  * Phase-progress reporting for the intelligence-build pipeline with UI phase updates
  * Short-circuit blueprint detection accepts boolean true and case-insensitive "true"

* **Tests**
  * Added/expanded tests for async task dispatch, handlers, watchdog, and blueprint connectivity

* **Refactor**
  * Centralized async-task dispatch used across multiple launch-pad handlers

<!-- review_stack_entry_start -->

[![Review Change Stack](https://storage.googleapis.com/coderabbit_public_assets/review-stack-in-coderabbit-ui.svg)](https://app.coderabbit.ai/change-stack/modernagencysales/mas-platform/pull/1234)

<!-- review_stack_entry_end -->
<!-- end of auto-generated comment: release notes by coderabbit.ai -->
