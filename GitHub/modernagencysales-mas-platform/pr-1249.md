---
type: pr
source: github
date: 2026-05-14
url: https://github.com/modernagencysales/mas-platform/pull/1249
title: "fix(copilot): unblock full lead-magnet flow (V2 content, begin_ allow, brand-kit scope)"
repo: modernagencysales/mas-platform
number: 1249
state: closed
author: Ajma1
---

# fix(copilot): unblock full lead-magnet flow (V2 content, begin_ allow, brand-kit scope)

PR #1249 in modernagencysales/mas-platform — closed — by Ajma1 — [https://github.com/modernagencysales/mas-platform/pull/1249](https://github.com/modernagencysales/mas-platform/pull/1249)

## Summary

Follow-up to #1247 — the previous PR fixed the loudest "Lead magnet not found" symptom (scope mismatch on `generate_lead_magnet_posts`). Production audit revealed three additional failure classes still blocking the lead-magnet co-pilot end-to-end.

| Failure | Surface | Fix |
|---|---|---|
| V2 unified `content` column unreachable from copilot reads (73/246 magnets are V2-only) → 400 "missing concept or extracted content" / "no content to refine" | `generatePosts`, `refine_lead_magnet` | Widen column whitelist, relax gate to require `concept` only, fall `proof` through `extracted_content` → `content` → `''` |
| `begin_lead_magnet_creation` stalled on "Action requires confirmation" (4 users, last 14d, most recent May 13–14) | `action-execution-policy` | Add `begin_` to `ALLOW_PREFIXES` (handler is read-only); pin allow-list in test |
| `loadBrandKit` filtered only by `userId` — team members not workspace owner got generic "Industry expert" / "Business professionals" copy | `loadBrandKit` | Accept `DataScope`; in team scope prefer brand_kit row matching `team_id`; back-compat with bare userId |

## Test plan
- [x] `pnpm exec vitest run lead-magnet-creation classify-policy actions/lead-magnets` — 50/50 (17 + 11 + 22)
- [x] `NODE_OPTIONS='--max-old-space-size=8192' pnpm exec tsc --noEmit` — clean
- [ ] Preview deploy:
  - Magnet `8f728db1-…` (content only): expect `post_preview`. Pre-fix: 400 "missing concept or extracted content".
  - Magnet `4373c8dd-…` (extracted only): expect `post_preview`. Pre-fix: 404 (scope bug, fixed in #1247).
  - Magnet `1381093d-…` (no content): expect new 400 "has no concept yet — finish the creation wizard". Truthful error.
  - Trigger `begin_lead_magnet_creation` from a fresh chat: confirmation chip must NOT appear; result streams directly.
- [ ] Ops watch: re-run the audit query below 7 days post-merge; failure count for `generate_lead_magnet_posts` + `begin_lead_magnet_creation` should drop to near zero.

```sql
select tool_name, tool_result->>'error' as error_msg, count(*) c
from public.copilot_messages
where role = 'tool_result' and created_at > now() - interval '14 days'
  and (tool_result->>'success')::boolean = false
  and tool_name ilike '%lead_magnet%'
group by 1, 2 order by c desc;
```

## Out of scope (separate PRs)
- **Backfill** the 5 published-but-empty magnets (data state, not code).
- **Drop legacy columns** (`extracted_content` / `generated_content` / `polished_content`) after a one-time normalization pass writes `content` for legacy-only rows.
- **Write-time guard** preventing `status='published'` with NULL content (belongs in `lead-magnets.service.ts` publish path).
- **`refine_lead_magnet` timeout** — 1 occurrence, infra/Claude, not a code bug.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

## Release Notes

* **New Features**
  * Added team-level brand kit support for Copilot workflows
  * Enhanced lead magnet handling for improved content compatibility

* **Bug Fixes**
  * Improved error messaging when lead magnet content is missing

* **Tests**
  * Added test coverage for lead magnet validation and action policy classification

<!-- review_stack_entry_start -->

[![Review Change Stack](https://storage.googleapis.com/coderabbit_public_assets/review-stack-in-coderabbit-ui.svg)](https://app.coderabbit.ai/change-stack/modernagencysales/mas-platform/pull/1249)

<!-- review_stack_entry_end -->

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
